import { Story, Preview, Meta, Props } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

import { text} from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';

# Storybook Docs for TemplateComponentName

<Meta title="DESIGN SYSTEM/template-component" />

## Story definition

Preview of default state.

<Story name="Develop">
  {() => {
		const knobs = {
			first: text('First', 'Jon'),
			last: text('Last', 'Snow')
		};
		return html`
			<tag-template-component
				first='${knobs.first}'
				last='${knobs.last}'
				@click=${action('TemplateComponentName click')}>
			</tag-template-component>
		`}
	}
</Story>

## API

<Props of="tag-template-component" />

## Examples

Basic example

<Preview withToolbar>
  <Story name="Basic">
    {html`
		  <tag-template-component
		    first='Jon'
		    last='Snow'>
		  </tag-template-component>
		`}
	</Story>
</Preview>
