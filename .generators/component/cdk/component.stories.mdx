import { Story, Preview, Meta, Props } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

import { customEventAction } from '../../../docs/utils/decorators.js';
import { action, decorate } from '@storybook/addon-actions';
import { text} from "@storybook/addon-knobs";

# Storybook Docs for TemplateComponentName

<Meta title="CDK/template-component" />

## Story definition

Preview of default state.

<Story name="Develop">
  {() => {
		const knobs = {
			first: text('First', 'Jon'),
			last: text('Last', 'Snow')
		};
		return html`
			<tag-template-component
				first='${knobs.first}'
				last='${knobs.last}'
				@click=${action('TemplateComponentName click')}
				@eventChange="${customEventAction('eventChange')}"
			>
			</tag-template-component>
		`}
	}
</Story>

## API

<Props of="tag-template-component" />

## Examples

Basic example

<Preview withToolbar>
  <Story name="Basic">
    {html`
		  <tag-template-component
		    first='Jon'
		    last='Snow'>
		  </tag-template-component>
		`}
  </Story>
</Preview>
