:host {
  /** Las variables deberán ser mapeadas a una con el mismo nombre pero privada (_) */
	--_cdk-{{dashCase name}}-background-color: 				var(--cdk-{{dashCase name}}-background-color, white);
}
